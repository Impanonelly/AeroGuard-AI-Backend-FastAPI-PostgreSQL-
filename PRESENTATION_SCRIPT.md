# AeroGuard AI: Exact Defense Presentation Script

This is your exact, click-by-click script for your graduation defense. Follow these literal steps while presenting to guarantee a perfect demonstration of the Scikit-Learn Machine Learning integration.

## Scene 1: Proving the AI is Real (In VS Code)
*Start your presentation with the browser closed, and VS Code open on the big screen.*

1. Open your backend terminal in VS Code that is running Uvicorn.
2. **Press `Ctrl + C`** to stop the FastAPI server.
3. **Type this exact command and press Enter:**
   ```bash
   venv\Scripts\python ai_engine\train.py
   ```
4. **What you say to the panel:** *"Ladies and Gentlemen, you are now watching the true AI engine compiling. The system is generating 10,000 synthetic pilot records and training a Scikit-Learn Random Forest Neural Model in real-time."*
5. **Point to the screen:** Show them the `[+] AI Model Accuracy Rating: 98%` line.
6. **Type this exact command and press Enter to restart your server:**
   ```bash
   uvicorn main:app --reload
   ```
7. **What you say to the panel:** *"The AI's brain has been successfully serialized into a `.pkl` file. Our FastAPI backend is now actively using this ML model to evaluate pilot fatigue instead of static human code."*

---

## Scene 2: The Live Test (In Google Chrome)
*Minimize VS Code and open your web browser.*

1. **Go to your frontend URL:** `http://localhost:3001/login`
2. **Click** the `Quick Demo Access` dropdown.
3. **Select** `Safety Manager` and click **Secure Login**.
4. Once on the Dashboard, **Click** `Personnel Health` on the left sidebar.
5. **Click** the blue `Add Personnel` button.
6. **Type the following exact details:**
   * **Full Name:** Capt. ML Tester
   * **Email:** mltester@aeroguard.com
   * **Employee ID:** AKG-TEST-99
   * **Role:** Line Pilot (Aviator)
7. **Click** `Confirm Registration`.
8. The system will instantly pop up a Green success message, and `Capt. ML Tester` will appear in the table.

---

## Scene 3: Forcing the AI to Intercept
*Now, prove the AI model we just trained actually evaluated the pilot you just added.*

1. While still on the `Personnel Health` page, **look at the row** for `Capt. ML Tester`.
2. **What you say to the panel:** *"When I created this pilot, the React Frontend sent their default biometrics (8 hours sleep, 0 duty hours, 0 stress) straight to the new Scikit-Learn Neural matrix. The AI analyzed it and dynamically declared him as FIT FOR DUTY with LOW risk."*
3. **Click** `Command Center` on the left sidebar.
4. Under the `Vitals Entry Form`, select `Capt. ML Tester` from the dropdown.
5. **Type extreme failure numbers into the form:**
   * **Heart Rate:** 120
   * **SpO2:** 90
   * **Sleep Hours:** `1.5`
   * **Fatigue Score:** `95`
6. **Click** `Submit Health Record`.
7. **Click** `Personnel Health` on the left sidebar again.
8. **Point to the screen:** The row for `Capt. ML Tester` will now be mathematically updated. His status will have flipped to **HIGH RISK (Immediate Action)** based on the AI Model recognizing the dangerous 1.5-hour sleep deficit pattern!
